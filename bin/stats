#!/usr/bin/python

import socket

PORT=31416

with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
    s.connect(("node05", PORT))
    s.sendall(b'<auth1/>\003')
    data = s.recv(1024)
print('Received', repr(data))
